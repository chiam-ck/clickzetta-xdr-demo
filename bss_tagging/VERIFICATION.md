# Verification — 2026-09-10

Passed Python syntax compilation and ten unittest cases. Tests used 100 synthetic
customers and 150 subscriptions, executing the SQL business logic locally and
checking it against an independent Python oracle.

Validated: cardinalities, tag additions/retractions, missing consent, multiple
bills/tickets without join multiplication, missing subscriptions, source deletion,
one-day lifecycle events, catalog fanout, cost arithmetic and the idle billing
floor, response parsing, and stop/log behavior on a simulated CLI error.

No real SQL failure was generated by the failure test: its CLI and findings file
were redirected to an isolated temporary fixture.

Remote tests are now complete on a dedicated one-CRU `BSS_BENCH` cluster at
100,000 and 1,000,000 customers. All 26 correctness snapshots matched the full
control. Five repeated 100-customer deltas per scale demonstrated incremental
refreshes; resource counters and server timings are captured. Edge cases include
payments, complaint closure, ageing, termination, catalog fanout and deletion.

At 1M, incremental CPU was 36% lower and output bytes 97.6% lower than full
rebuilding, but the five-refresh chain took longer. The 80% compute-reduction
gate was not met. A large catalog fanout caused full downstream refreshes.
Account-specific rates are omitted; experiment invoice attribution is pending.
The cluster is confirmed suspended and no BSS schedule is enabled.
See [MEASURED_REPORT.md](MEASURED_REPORT.md) and [measured_results.csv](measured_results.csv).

Generated the default review plan at `out/bss_tagging_100k/plan.json`: 100,000
customers, 150,000 subscribers/subscriptions, 100 selected changed customers,
fresh target schema `bss_tagging_100k`. The generated directory is git-ignored;
source templates and the generator are versionable. The illustrative cost command
returned USD104.16 for 30 days at 1 CRU, 10 seconds work + 60 seconds idle tail,
15-minute cadence and USD1.86/CRU-hour. This is an arithmetic check, not telemetry.
